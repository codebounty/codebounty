var Scheduler = (function () {
    var my = {};

    var cronJob = require('cron').CronJob;

    var initialize = function () {
        //load all the jobs from the database

    };

    //TODO figure out a way to make certain this no get hacked
    my.addPayment = function (bounty) {

    };

    initialize();

    return my;
})();